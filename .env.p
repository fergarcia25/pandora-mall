# Pandora Mall — Environment Variables (example)
# Copy this to .env to use locally (already done by default)
VITE_API_URL=http://localhost:3001/api
VITE_BASE_URL=/

# For production, see .env.production.example
